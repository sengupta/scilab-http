function response = http_get(host, resource, port, varargin)
// varargin is the list of parameters to be sent using the GET request
    SOCKET_open(count, host, port) // The "count" variable has to be an incremented global
endfunction

